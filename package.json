{
  "name": "ip alpha",
  "version": "0.0.0",
  "lib": [
    "ES2020",
    "WebWorker"
  ],
  "scripts": {
    "build": "cd src && tsc && cd .. && ts-node inject.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^2.1.0",
    "@types/mkdirp": "^1.0.2",
    "@types/node": "^14.14.21",
    "@types/rimraf": "^3.0.1",
    "mkdirp": "^1.0.4",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1"
  }
}
