{
  "name": "ip alpha",
  "version": "0.0.0",
  "lib": [
    "ES2020",
    "WebWorker"
  ],
  "scripts": {
    "build": "cd src && tsc && cd .. && ts-node inject.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^2.1.0",
    "@types/node": "^14.14.21",
    "ts-node": "^9.1.1"
  }
}
